#include <stdio.h>

int main() {
    int n = 10;   
    int f0 = 0, f1 = 1, f2 = 0;  

    printf("%d ", f0);   
    printf("%d ", f1); 
    printf("%d ", f2);

    for (int i = 2; i < n; i++) {
        f2 = f0 + f1;          
        f0 = f1;             
        f1 = f2;
        printf("%d ", f2);
    }

    return 0;
}
